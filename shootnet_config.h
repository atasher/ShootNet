#ifndef SHOOTNET_CONFIG_h
#define SHOOTNET_CONFIG_h

class IPAddress;

extern const char* ssid;     // The SSID (name) of the Wi-Fi network you want to connect to
extern const char* password; // The password of the Wi-Fi network

extern const IPAddress CONTROLLER_IP; // the fixed IP address of the controller

#endif